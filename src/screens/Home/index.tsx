import { fonts } from '@styles/fonts';
import { Text, View } from 'react-native';

const Home = () => {

  return (
    <View>
      <Text style={fonts().bold18}>Home</Text>
    </View>
  );
};

export default Home;
